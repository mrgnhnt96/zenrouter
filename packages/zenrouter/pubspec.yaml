name: zenrouter
description: "The Flutter router that unifies Navigator 1.0 and 2.0 into one elegant system"
version: 0.1.0
homepage: https://github.com/definev/zenrouter/packages/zenrouter

environment:
  sdk: ^3.8.0
  flutter: ">=3.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: