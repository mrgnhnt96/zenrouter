name: zenrouter_file_generator_example
description: "An example project for zenrouter_file_generator."
publish_to: 'none'
version: 0.1.0+1

resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  
  zenrouter: ^0.2.1
  zenrouter_devtools: ^0.2.0
  zenrouter_file_generator:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  build_runner: ^2.10.4

flutter:
  uses-material-design: true
